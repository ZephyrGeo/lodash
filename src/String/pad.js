import repeat from './repeat.js'

const pad = (string = '', length = 0, chars = ' ') => {
    const paddingLength = length - string.length
    const leftPadding =
}

pad('abc', 9, '_+')

console.log(3 / 2, 3 % 2)
